# 更新日志

本文件记录「无线麦 SayAll」Windows 版的版本变更。遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.11] - 2026-09-19

### 变更
- **「方案」独立成一页**：预设方案、按应用自动切换、按键保持、导入/导出/恢复默认从「按键」页搬到侧栏新增的「方案」页，统一「左边一句说明、右边一组控件」的版式。按键页只剩遥控器示意图与编辑面板，头部留一个「方案设置…」入口。
- **画布外按键改版**：静音（以及 Google TV 遥控器的 YouTube / Netflix）原本是「一个标签 + 三个撑满整行的大格子」，现改为与示意图旁一模一样的卡片——图标、名称、单击/双击/长按三列，按窗口宽度自适应排列，选中与按下的高亮也与画布一致。
- **准备清单不再硬卡虚拟声卡**：虚拟声卡与语音工具快捷键两项新增「我已确认可以使用」按钮。装了 VB-CABLE 但没被枚举到（或用的语音工具压根不需要快捷键）时，点一下就按完成处理，错误码同时收起；随时可以撤销回到按检测结果判断。确认状态写进应用设置，重启后保留。
- **准备完成后自动收起「准备」页**：必需项第一次全部满足后，侧栏不再常驻「准备」，入口移到「关于」页的「准备清单 → 打开准备清单」。手动打开时侧栏会临时把它显示出来，离开该页即收起；之后遥控器临时断开也不会再把这一页弹回侧栏。

### 内部
- 新增 `readiness_confirmed_items`、`readiness_completed` 两个设置字段与对应的三个 IPC 命令，状态变更全部带结构化日志；旧设置文件缺字段按默认值读入。

## [0.2.10] - 2026-09-16

### 修复
- **双击有时候不认**：双击判定窗口不再硬编码 300 毫秒，改为跟随你在 Windows 里设置的双击速度（夹在 0.5–0.9 秒）。蓝牙链路的按键抖动比有线键盘大，固定 300 毫秒在真机上会把自然双击判成两次单击。
- **部分程序收不到映射的按键**：注入的按键现在按下后保持一小段时间再松开（默认 30 毫秒，映射页可调 0–1000）。原先按下与松开是同一瞬间发出的，轮询键盘状态的程序（游戏、部分 Electron 应用）会整个漏掉。

### 新增
- **动作序列**：每个触发格可以配置一串按顺序执行的动作（最多 8 步），并可插入等待步骤，例如「输入文字 → 等 30 毫秒 → 回车」。
- **从输入法读取语音热键**：搜狗和豆包的语音热键可以一键读取并套用，不必两边手动对齐。微信输入法没有可读的配置，仍需手动核对。
- **按应用自动切换方案**：绑定「应用 → 预设方案」，切到该应用自动套用，离开后回到你保存的配置。自动切换只是临时覆盖，不会改写你保存的映射。
- **遥控器电量**：连接后在准备页显示。
- **准备页错误码**：未就绪项给出错误码，报障时带上这个码即可。
- **语音期间临时切换默认麦克风**（默认关闭）：开启后按住语音键期间自动把系统默认麦克风切到虚拟声卡，松开还原，不必在语音工具里改麦克风。会影响同时在录音的程序，且依赖未公开的系统接口，请先自行验证再长期使用。
- **按键动作类型扩容**：新增鼠标按键、鼠标滚轮（按住连滚，轻点滚一格）、文本输出、按住快捷键（遥控器按多久就按住多久）。上/下映射成滚轮即可用遥控器滚网页。
- **预设方案**：通用 / 阅读 / 影音三套整键位，一键套用，不必逐键点。
- **语音工具预设扩到 6 条**：微信输入法、Windows 语音输入、千问、搜狗、豆包、关闭，每条带「已验证 / 未验证 / 已知不可用」标注。
- **语音增强**（默认关闭）：高通滤波去掉低频漂移与隆隆声，自动增益把音量调平，软限幅防削顶。开启后由自动增益接管电平，对下一段语音生效。
- **首启「准备」页**：遥控器 / 虚拟声卡 / 语音输出设备 / 语音工具快捷键 / 按键映射五项的状态与操作按钮集中一屏，从上往下点一遍即可用。
- **应用内运行日志**：权限页可直接读取日志末尾、打开所在文件夹、清空，不必去翻 LocalAppData。
- **遥控器机型档案**：设备识别、按键集合与 HID usage 表按机型分表，接入 Google TV / Chromecast 语音遥控器（**尚未真机验收**，待验证项见 `docs/investigations/2026-09-15-google-tv-remote-profile.md`）。
- **静音键可配置**：该键一直可解码，但遥控器示意图上没有位置，现由「该遥控器的其他按键」补出。

### 变更
- 按键映射文件遇到不认识的动作类型时降级为「未配置」，不再整份读取失败。
- 应用设置 schema 升到 4（旧文件缺字段按默认值读入）。

## [0.2.9] - 2026-09-13

### 修复
- **RC003 返回/音量± 键终于生效**：承载遥控器 HID-over-GATT 的 WUDFHost 是写受限（write-restricted）LOCAL SERVICE 进程，钩子在装 Detours 时遍历宿主自身线程，其中个别线程 `OpenThread` 会被拒（`ACCESS_DENIED`），旧逻辑将其当致命错误、整体放弃装钩，导致返回/音量± 一直无效。现改为跳过开不了的线程、并始终修正当前线程，钩子可靠安装。
- **钩子 DLL 自卸载，消除宿主内存泄漏**：旧实现每次连接都向 WUDFHost 注入一份永久驻留（PIN）的钩子 DLL，长期累积。现改为干净停止时排空在途回调后自卸载，宿主内不再累积；磁盘暂存副本同步清理。
- **自启动任务注册失败修复**：管理员登录自启动计划任务改由应用首次以管理员身份运行时自行注册。此前由 currentUser（非提权）安装器创建，因权限不足失败（错误 `0x80004005` / `-2147467259`）；现应用已强制提权，创建可靠成功。

### 变更
- **应用强制以管理员身份运行**：注入 WUDFHost 需要管理员权限，应用现内嵌 `requireAdministrator` 清单，启动即请求提权，无需再手动「以管理员身份运行」。
- **钩子仅在遥控器连接时驻留**：遥控器断开 / 挂起 / 重连 / 断流时自动摘钩并卸载注入 DLL（配合本版自卸载，宿主 WUDFHost 内不残留注入模块），最大限度降低被安全/反作弊软件误判的窗口。
- **诊断日志体积封顶**：单文件超过 5 MiB 自动滚动，仅保留一份 `.1` 备份（磁盘占用约 10 MiB 封顶），不再无限增长常驻磁盘。

### 诊断
- 钩子安装失败时输出结构化诊断（`state` / `magic` / `hook_error` / 工作线程退出码 / 线程计数），便于一次日志定位。

[0.2.10]: https://github.com/Suk-ldev/remote-mic-app-windows/releases/tag/v0.2.10
[0.2.9]: https://github.com/Suk-ldev/remote-mic-app-windows/releases/tag/v0.2.9
